step1:

<link rel="stylesheet"
href="<%=path%>/js/bootstrap-table/bootstrap-table.min.css"></link>

<script type="text/javascript"
src="<%=path%>/js/bootstrap-table/bootstrap-table.min.js"></script>



step2:

<table id="table"></table>

step3:

function initMyTable(){
	$("#table").bootstrapTable({ // 对应table标签的id
	      url: "<%=request.getContextPath()%>/json/data.json", // 获取表格数据的url
	      striped: true,  //表格显示条纹，默认为false
	      toolbar:'#toolbar',
	      pagination: true, // 在表格底部显示分页组件，默认false
	      pageList: [1, 2,3], // 设置页面可以显示的数据条数
	      pageSize: 2, // 页面数据条数
	      pageNumber: 1, // 首页页码
	      clickToSelect: true, //是否启用点击选中行
	      sidePagination: 'server', // 设置为服务器端分页(!!!!)
	      queryParams: function (params) { // 请求服务器数据时发送的参数，可以在这里添加额外的查询参数，返回false则终止请求
				console.log(params)
		  return {
		      pageSize: params.limit, // 每页要显示的数据条数
		      offset: params.offset, // 每页显示数据的开始行号
		      dataId: $("#dataId").val() // 额外添加的参数
		  }
	      },
	      columns: [
		  {
		      checkbox: true, // 显示一个勾选框
		      align: 'center' // 居中显示
		  }, {
		      field: 'id', // 返回json数据中的name
		      title: 'id', // 表格表头显示文字
		      order:'desc',
		      align: 'center', // 左右居中
		      valign: 'middle' // 上下居中
		  }, {
		      title: "操作",
		      align: 'center',
		      valign: 'middle',
		      width: 160, // 定义列的宽度，单位为像素px
		      formatter: function (value, row, index) {
			  return '<button class="btn btn-primary btn-sm" >删除</button>';
		      }
		  }
	      ],
	      onLoadSuccess: function(){  //加载成功时执行
		    console.info("加载成功");
	      },
	      onLoadError: function(){  //加载失败时执行
		    console.info("加载数据失败");
	      }
	})
}


刷新方法
$('#table').bootstrapTable('refresh');